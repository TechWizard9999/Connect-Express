{
    "name": "connect-express-root",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "install-all": "npm install --prefix backend && npm install --prefix frontend",
        "build-frontend": "cd frontend && npm install && npm run build",
        "start-backend": "cd backend && npm install && npm start"
    }
}